let numero = 20
let numero2 = 10

function media (numero, numero2) {
    return (numero + numero2) / 2
}

media = media (numero, numero2)

console.log(`A média entre ${numero} e ${numero2} é: ${media}`)